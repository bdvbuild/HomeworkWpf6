<Window x:Class="HomeworkWpf6.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HomeworkWpf6"
        mc:Ignorable="d"
        Title="Погода" Height="250" Width="300">
    <!--<Window.Resources>
        <local:WeatherControl Temp="25" WindDirection="СВ" WindSpeed="5" Precipitation="Sunny" x:Key="Wheather1" />
    </Window.Resources>-->
    <Grid>
        <!--x:Name="grid1" DataContext="{StaticResource Weather1}">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <TextBlock Text="Скорость ветра" />
        <TextBlock Text="{Binding WindSpeed}" Grid.Column="1"  />
        <TextBlock Text="Температура" Grid.Row="1" />
        <TextBox Text="{Binding Temp, Mode=TwoWay}" Grid.Column="1" Grid.Row="1" TextChanged="TextBox_TextChanged"  />
        <Button Content="Check" Grid.Row="2" Grid.Column="2"/>-->    
    </Grid>
</Window>
